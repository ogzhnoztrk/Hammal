@using Hammal.Models;

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h4 class="text-primary">Job Category List</h4>
        </div>
        <div class="col-6  text-end">
            <a asp-controller="Advertisement" asp-action="Upsert" class="btn btn-primary"> <i class="bi bi-plus-circle"></i> &nbsp; New Category</a>
        </div>
    </div>
    <br />
    <br />

    <table id="tblData" class="table table-bordered table-striped">
        <thead>
            <tr>
                <th> Title</th>
                <th> Category Name </th>              
                <th> ReleaseDate</th>
                <th> LastDate</th>
                <th> Edit </th>
            </tr>
        </thead>
       @* <tbody>
            @foreach (var category in Model)
            {

                <tr>
                    <td>
                        @category.Id
                    </td>
                    <td>
                        @category.Name
                    </td>
                    <td>
                        <div class="w-75 btn btn-group" role="group">
                            <a asp-action="Upsert" asp-route-id="@category.Id" class="btn btn-primary mx-2">
                                <i class="bi bi-pencil-square"></i>
                                Edit
                            </a>
                            <a class="btn btn-danger mx-2" id="delete">
                                <i class="bi bi-trash-fill"></i>
                                Delete
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>*@
    </table>
</div>


@section Scripts{
    <script src="~/js/advertisement.js"></script>
}